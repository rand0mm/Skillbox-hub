if (condition) {
  // код, выполняющийся при соблюдении условия
}

if (condition) {
  // код, выполняющийся при соблюдении условия
} else {
  // код, выполняющийся в противном случае
}

if (condition) {
  // при первом условии
} else if (condition2) {
  // при втором условии
} else if (condition3) {
  // при третьем условии
} else {
  // если ниодно условие не соблюдено
}